portraits = {
	# Compatibility portrait restored for legacy create_species/species_class/portrait_set references.
	# Uses an existing NYTO chibi asset because legacy EILD DDS files are absent.
	GFnyto_eild = { texturefile = "gfx/models/portraits/NYTO/chibi/GF_nyto_chibi_Isomer_black.png" }
}

portrait_groups = {
	# Compatibility group retained for existing EILD species class and portrait_set references.
	GFnyto_eild = {
		default = GFnyto_eild
		species = { add = { portraits = { GFnyto_eild } } }
		pop = { add = { portraits = { GFnyto_eild } } }
		pop_group = { add = { portraits = { GFnyto_eild } } }
		leader = { add = { portraits = { GFnyto_eild } } }
		ruler = { add = { portraits = { GFnyto_eild } } }
	}
}
